---
title: "functional"
sidebar_label: "functional"
description: "Implements higher-order functions.  You can import these APIs from the `algorithm` package. For example:  ```mojo from std.algorithm import map ```"
---

# functional

Implements higher-order functions.

You can import these APIs from the `algorithm` package. For example:

```mojo
from std.algorithm import map
```

## 函数

### _get_start_indices_of_nth_subvolume

`@always_inline def _get_start_indices_of_nth_subvolume[
    rank`

### elementwise

`@always_inline def elementwise[
    func`

### elementwise

`@always_inline def elementwise[
    rank`

### func_unified

`def func_unified[
        width`

### elementwise

`@always_inline def elementwise[
    func`

### elementwise

`@always_inline def elementwise[
    rank`

### func_unified

`def func_unified[
        width`

### elementwise

`@always_inline def elementwise[
    rank`

### description_fn

`@always_inline @parameter def description_fn() -&gt; String`

### gpu_func_unified

`def gpu_func_unified[
                width`

### cpu_func_unified

`def cpu_func_unified[
                width`

### _elementwise_impl

`@always_inline def _elementwise_impl[
    rank`

### description_fn

`@always_inline @parameter def description_fn() -&gt; String`
