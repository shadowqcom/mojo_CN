---
title: "index"
sidebar_label: "index"
description: "Implements `IndexList` which is commonly used to represent N-D indices.  You can import these APIs from the `utils` package. For example:  ```mojo from std.utils import IndexList ```"
---

# index

Implements `IndexList` which is commonly used to represent N-D
indices.

You can import these APIs from the `utils` package. For example:

```mojo
from std.utils import IndexList
```

## 类型

### IndexList

`struct IndexList[size`

## 函数

### _reduce_and_fn

`@always_inline def _reduce_and_fn(a`

### _int_tuple_binary_apply

`@always_inline def _int_tuple_binary_apply[
    binary_fn`

### _int_tuple_compare

`@always_inline def _int_tuple_compare[
    comp_fn`

### _bool_tuple_reduce

`@always_inline def _bool_tuple_reduce[
    reduce_fn`

### _type_of_width

`def _type_of_width[bitwidth`
