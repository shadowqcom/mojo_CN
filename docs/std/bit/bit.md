---
title: "bit"
sidebar_label: "bit"
description: "Provides functions for bit manipulation.  You can import these APIs from the `bit` package. For example:  ```mojo from std.bit import count_leading_zeros ```"
---

# bit

Provides functions for bit manipulation.

You can import these APIs from the `bit` package. For example:

```mojo
from std.bit import count_leading_zeros
```

## 函数

### count_leading_zeros

`def count_leading_zeros(val`

### count_leading_zeros

`def count_leading_zeros[
    dtype`

### count_trailing_zeros

`def count_trailing_zeros(val`

### count_trailing_zeros

`def count_trailing_zeros[
    dtype`

### bit_reverse

`def bit_reverse(val`

### bit_reverse

`def bit_reverse[
    dtype`

### byte_swap

`def byte_swap(val`

### byte_swap

`def byte_swap[
    dtype`

### pop_count

`def pop_count(val`

### pop_count

`def pop_count[
    dtype`

### bit_not

`def bit_not[
    dtype`

### bit_width

`def bit_width(val`

### bit_width

`def bit_width[
    dtype`

### log2_floor

`@always_inline def log2_floor(val`

### log2_floor

`@always_inline def log2_floor[
    dtype`

### log2_ceil

`@always_inline def log2_ceil(val`

### log2_ceil

`@always_inline def log2_ceil(val`

### next_power_of_two

`@always_inline def next_power_of_two(val`

### next_power_of_two

`@always_inline def next_power_of_two[
    dtype`

### prev_power_of_two

`@always_inline def prev_power_of_two(val`

### prev_power_of_two

`@always_inline def prev_power_of_two[
    dtype`

### rotate_bits_left

`@always_inline def rotate_bits_left[shift`

### rotate_bits_left

`def rotate_bits_left[
    dtype`

### rotate_bits_right

`@always_inline def rotate_bits_right[shift`

### rotate_bits_right

`def rotate_bits_right[
    dtype`
