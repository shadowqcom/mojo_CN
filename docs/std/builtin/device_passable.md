---
title: "device_passable"
sidebar_label: "device_passable"
description: "Implements the `DevicePassable` trait for types transferable to accelerator devices."
---

# device_passable

Implements the `DevicePassable` trait for types transferable to accelerator devices.

## Traits

### DevicePassable

`trait DevicePassable`

**Methods:**

- **_is_convertible_to_device_type**: `@staticmethod def _is_convertible_to_device_type[SrcT`

- **_to_device_type**: `def _to_device_type(self, target`

- **get_type_name**: `@staticmethod def get_type_name() -&gt; String`
